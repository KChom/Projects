import requests
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

# API endpoint (replace with a live API providing updated data)
url = "https://api.example.com/economic-data"

# Function to fetch data
def fetch_economic_data():
    response = requests.get(url)
    if response.status_code == 200:
        data = response.json()
        return data
    else:
        print("Failed to fetch data")
        return None

# Fetch and process data
economic_data = fetch_economic_data()

if economic_data:
    # Process the data into a DataFrame
    df = pd.DataFrame(economic_data)

    # Normalize data here (optional, depending on your data)
    # Calculate EDI, etc. (use your formula)

    # Generate the heatmap
    plt.figure(figsize=(10, 6))
    sns.
